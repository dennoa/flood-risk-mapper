import {
  it,
  fit,
  describe,
  ddescribe,
  expect,
  inject,
  beforeEachProviders
} from '@angular/core/testing';

describe('Api Service', () => {

  beforeEachProviders(() => []);

  it('should ...', inject([], (api:any) => {

  }));

});
